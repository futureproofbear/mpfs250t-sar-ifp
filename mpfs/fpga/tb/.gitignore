# ModelSim build artifacts
work/
workF/
rwork/
fwork/
mwork/
dwork/
dmwork/
mut/
gwork/
workbase/
mut_*/
transcript
*.wlf
vsim.dbg

# Test vectors -- regenerated by `python gen_window_vectors.py` (deterministic).
# Not committed so the generator stays the single source of truth for the arithmetic.
win_in.hex
win_exp.hex
win_tab.hex
win_scale.hex
win_dims.vh

# Test vectors -- regenerated by `python gen_resample_vectors.py` (deterministic).
rs_mem.hex
rs_tab.hex
rs_cfg.hex
rs_exp.hex
rs_idx.hex
rs_wq.hex
rs_dims.vh

# Test vectors -- regenerated by `python gen_gather_vectors.py` (deterministic).
ga_mem.hex
ga_tab.hex
ga_exp.hex
ga_cfg.hex
ga_dims.vh

# ID converter TB (deterministic, no generated vectors)
iwork/
i2work/
mut3/
